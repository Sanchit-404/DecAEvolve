# Base conda env: create with `conda env create -f environment.yml` then install locked pip deps.
# The lock file was captured on Linux + CUDA 12; adjust if you use a different stack.
name: llmsr
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  # After create: pip install -r requirements-lock.txt
